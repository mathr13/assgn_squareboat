import 'package:assgn_news_squareboat/constants/sizes.dart';
import 'package:flutter/material.dart';

abstract class SBRadiuses {

  static const BorderRadius borderRadius1 = BorderRadius.all(Radius.circular(SBSizes.borderRadiusSize1));
  static const BorderRadius borderRadius2 = BorderRadius.all(Radius.circular(SBSizes.borderRadiusSize2));
  static const BorderRadius borderRadius3 = BorderRadius.all(Radius.circular(SBSizes.borderRadiusSize3));

}